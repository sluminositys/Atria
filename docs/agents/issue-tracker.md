# Issue Tracker: GitHub

Issues and PRDs for Atria live in the GitHub repository inferred from `git remote -v`.

Use `gh issue create`, `gh issue view`, `gh issue list`, `gh issue comment`, and `gh issue edit` for issue operations. Do not create or update remote issues unless the user explicitly requests it.

When an engineering skill says to publish to the issue tracker, create a GitHub issue in this repository.
